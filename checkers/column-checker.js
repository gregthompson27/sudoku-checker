const checkColumn = (grid, columnNumber) => {
  let flag = true;

  return flag;
};

const checkAllColumns = (grid) => {
  let flag = true;

  return flag;
};

module.exports = {
  checkColumn,
  checkAllColumns,
};
