const checkBox = (grid, boxNumber) => {
  let flag = true;

  return flag;
};

const checkAllBoxes = (grid) => {
  let flag = true;

  return flag;
};

module.exports = {
  checkBox,
  checkAllBoxes,
}