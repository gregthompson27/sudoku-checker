const checkRow = (grid, rowNumber) => {
  let flag = true;

  return flag;
};

const checkAllRows = (grid) => {
  let flag = true;

  return true;
};

module.exports = {
  checkRow,
  checkAllRows,
};
